# 프로젝트 기획 문서 (project.md)

## 대회 정보
- **공모전**: Google Cloud × Solana Foundation × Superteam Korea 공동 주최
- **주제**: Agentic Commerce — AI 에이전트가 사람 승인 없이 예산 한도 안에서 스스로 결제·정산·서명
- **제출 마감**: 2026년 8월 3일
- **선택 트랙**: **B. Autonomous On-chain Settlement**
  - "정책·예산 한도 내 에이전트가 직접 지갑 서명·결제"
  - 핵심 심사 포인트: 한도 초과 시,카테고리가 벗어날 시 결제를 자동 거부하는 흐름을 데모로 증명

## 핵심 기술 개념
- **x402**: HTTP 402 상태코드 기반 결제 요청 표준. 가격을 물어보는 요청 자체는 무료 — 실제 결제는 서명 후 재요청할 때만 발생
- **Pay.sh**: Google Cloud + Solana Foundation이 만든 오픈소스 결제 프록시 (에이전트-클라우드 사이 중개)
- **Solana**: 실제 USDC 이체가 일어나는 블록체인 네트워크. 가격을 정하는 게 아니라 이체만 실행하는 인프라
- **USDC**: 결제에 쓰는 스테이블코인

## 최종 아키텍처 (확정)
```
1. 사용자 → 백엔드: 작업 요청
2. 백엔드 → Google Cloud API: 호출 (가격 확인용, 아직 결제 전)
3. Google Cloud → 백엔드: HTTP 402(x402) 응답, 실제 확정 가격 전달
4. 백엔드 → AI 로직: 확정 금액 + 카테고리 전달, 정책 판단 요청
5. AI 로직: 정책 판단 (한도/카테고리, 실제금액 기준 — 단 한 번, 정확하게)
   - 거부 → 백엔드가 사용자한테 거부 알림, 여기서 끝
   - 승인 → 6번으로
6. 백엔드 → Solana: 지갑 서명 + USDC 트랜잭션 전송
7. Solana → 백엔드: 트랜잭션 확정 (온체인)
8. 백엔드 → Google Cloud: 결제완료 증빙 제출
9. Google Cloud → 백엔드: 실제 API 응답 반환
10. 백엔드 → 사용자: 최종 결과 전달
```

**핵심 포인트**: x402 가격 확인이 무료라서(Google AI Studio와 Gemini API는 실제 텍스트나 이미지를 생성하기 전에 프롬프트의 토큰 크기를 미리 계산해 주는 Count Tokens API를 별도로 제공), 추정치 판단 없이 확정 금액 기준으로 정책 판단을 **한 번만** 하면 됨. 불필요한 재검증 단계 없앰.

## 팀 역할 (B트랙 기준, 5일 스프린트)

| 역할 | 담당 | 핵심 작업 |
|---|---|---|
| 백엔드/인프라 | 게임 친구 | Pay.sh 연동, Google Cloud API 호출(가격확인+실행), 솔라나 지갑 서명·USDC 트랜잭션 실행 |
| **AI 에이전트 로직** | **나 (AI 친구)** | **정책 판단 엔진 — 확정금액/카테고리 받아서 승인·거부 JSON 리턴** |
| 프론트/데모·피칭 | 로봇 친구 | 전체 흐름 시각화, "한도초과 자동거부" 데모 시나리오, 피칭덱 |
| 보안·결제한도 설계 | 정보보안 친구 | 정책 규칙(숫자) 확정, 지갑 키 보관 방식, 체크리스트 (하루 안에 완결 후 대기) |

## 역할 간 데이터 흐름 (병목 주의)
```
정보보안 친구 → 정책 규칙(숫자) → 나(AI 친구)
백엔드 → Google Cloud: 가격 확인 (402 응답)
백엔드 → 나(AI 친구): 확정금액 + 카테고리 전달
나(AI 친구) → 승인/거부 JSON → 백엔드
백엔드 → 실제 서명·결제 → 온체인 결과
온체인 결과 → 로봇 친구 → 화면 표시
```
Day1에 이 연결고리(정책규칙 전달, JSON 포맷 합의) 안 끝나면 전체 스프린트 밀림.

## 내가(AI 친구) 만들어야 하는 것
1. **정책 판단 로직**: 카테고리 화이트리스트, 건당 한도, 일일 누적 한도, 호출횟수 체크
2. **입력**: 백엔드로부터 확정 금액(x402 402 응답에서 나온 실제 가격) + 카테고리 (+ 아래 거부조건 확장으로 추가된 상태값들)
3. **출력 포맷** (백엔드와 합의):
   ```json
   {
     "approved": true,
     "amount": 0.005,
     "category": "gemini",
     "reason": "모든 정책 통과",
     "policy_check": ["category_ok", "per_tx_limit_ok", "daily_limit_ok", "call_count_ok"],
     "timestamp": "...",
     "spent_today_after": 0.005
   }
   ```
4. **2단계 구조**:
   - 레이어1 (완성): 하드 규칙 if-else 체크
   - 레이어2 (완성): LLM(Gemini) 기반 의미 판단

## 보안 담당 파트너가 확정한 거부(reject) 조건 14가지
보안 담당 친구가 정의한 조건. 판단 방식에 따라 두 레이어로 분리해서 구현함.

**레이어1 (하드 규칙, `policy_engine.py`, `PaymentRequest` 상태값으로 판단)**
| 번호 | 조건 | 구현 코드 |
|---|---|---|
| 2 | 허용되지 않는 서비스(카테고리) 사용 | `category_fail` |
| 3 | API 미등록/미연결 (키 없음·오류) | `api_not_registered_fail` |
| 4 | 지갑 미연결 | `wallet_not_connected_fail` |
| 5 | 지갑 잔액 부족 | `insufficient_balance_fail` |
| 6 | 허용되지 않은 결제 대상(공식 주소 불일치) | `recipient_mismatch_fail` |
| 7 | 같은 작업 중복 결제 (request_id 중복) | `duplicate_request_fail` |
| 8 | 요청 횟수 과다 (분당 레이트리밋) | `rate_limit_fail` |
| 9 | AI 반복 실패 | `ai_repeated_failure_fail` |
| 13 | 필요 데이터/권한 없음 | `missing_permission_fail` |
| 14 | API/블록체인 상태 불안정 | `infra_unstable_fail` |

**레이어2 (LLM 의미 판단, `semantic_layer.py`, Gemini 호출, `user_prompt` 필요)**
| 번호 | 조건 | 구현 코드 |
|---|---|---|
| 1 | 프로젝트 목적과 무관한 요청 | `out_of_scope_fail` |
| 10 | 프롬프트 인젝션 감지 | `prompt_injection_fail` |
| 11 | 개인정보/비밀정보 요청 감지 | `sensitive_info_fail` |
| 12 | 사용자 목표 불분명 | `unclear_goal_fail` |

- `user_prompt`가 비어 있으면 레이어2는 생략됨 (`semantic_skipped`)
- 레이어1을 통과한 요청만 레이어2로 넘어감 (LLM 호출 비용 절약)
- 레이어2(Gemini) 호출 자체가 실패하면(예: `GEMINI_API_KEY` 미설정) 조건14(인프라 불안정)로 간주해 **자동 거부**(fail-safe) — 실행하려면 환경변수 `GEMINI_API_KEY` 필요
- `PaymentRequest` 입력 스키마가 `amount`, `category` 외에 `wallet_connected`, `wallet_balance`, `api_key_valid`, `recipient_address`, `request_id`, `ai_consecutive_failures`, `has_required_permission`, `infra_stable`, `user_prompt`, `task_plan`로 확장됨. 대부분 기본값이 "통과"로 되어 있어 백엔드가 필드를 안 보내도 동작은 하지만, 실제 데모에서는 백엔드가 실제 상태값을 채워서 보내야 정확한 판단이 됨. 필드별 상세(언제·어디서 값을 채워야 하는지)는 [BACKEND_연동_가이드.md](BACKEND_연동_가이드.md) 참고

## 레이어2 재설계 — "사람의 채팅"이 아니라 "AI의 자기 근거"를 심사하는 구조로 전환
초기엔 `user_prompt`를 "사람이 채팅창에 입력한 문장"으로 가정하고 few-shot을 짰는데,
이 프로젝트의 실제 위협 모델은 그게 아니라는 걸 재확인함:
- 실제 플로우에서 `user_prompt`는 **체인 중간의 AI가 매 호출마다 스스로 생성하는 작업 근거(justification) 텍스트**임
- 방어 대상은 "사람의 사회공학적 유도"가 아니라, **AI 스스로 범위를 넓히거나, 외부 콘텐츠(문서/API 응답)에 섞인 지시를 따르거나, 근거 없이 같은 작업을 반복하며 결제를 만들어내는 것**
- 이에 맞춰 `_PROJECT_SCOPE`와 few-shot 예시 7개를 전부 "AI가 생성한 호출 근거"를 판단하는 상황으로 재작성 (`semantic_layer.py`)
- `goal_clear` 판단 기준을 "요청이 명확한가"에서 **"이 호출이 왜 지금 필요한지 구체적 근거(무엇이 부족했는지, 몇 번째 시도인지)가 있는가"**로 재정의 → 무분별한 반복 호출(루프) 탐지가 핵심 역할이 됨

**`task_plan` 필드 추가 (인젝션 타이밍 방어)**
- 작업 시작 시점(=AI가 외부 콘텐츠를 읽기 **전**)에 확정된 계획 문자열을 매 하위 호출마다 동일하게 실어 보내는 필드
- 레이어2가 개별 호출의 근거를 이 계획과 대조해서, 계획에 없는 단계를 시도하면 범위 이탈/인젝션 신호로 판단
- 인젝션은 보통 실행 도중(외부 콘텐츠를 읽는 시점) 발생하므로, 계획을 그 전에 고정해두면 타이밍상 방어가 됨
- **재귀 호출 종료 방식**: 처음엔 "태스크당 최대 호출 횟수"로 하드캡을 두려다가, 그건 프롬프트 내용과 무관하게 임의의 숫자로 자르는 방식이라 방향성이 안 맞다고 판단해 폐기함. 대신 `task_plan`을 **유한한 단계 목록**(예: "1) A, 2) B, 3) C")으로 작성하게 하고, 매 호출이 몇 번째 단계인지 명시하게 해서, 계획에 남은 단계가 없는데 또 호출하면 레이어2가 거부하는 방식으로 "목표 달성 시 자연 종료"를 구현하는 방향으로 결정함. ⚠️ **아직 `semantic_layer.py` 프롬프트에 이 판단 로직을 명시적으로 강화하는 작업은 진행 중(미완성)**

**모델 선정**
- `gemini-2.5-flash`가 신규 사용자에게 차단된 걸 확인 → API로 실제 사용 가능한 모델 목록 조회 후 `gemini-3.1-flash-lite`로 확정
- "최신 모델 별칭(latest)"은 가격이 조용히 바뀔 수 있어 기각, 버전 고정 방식 채택
- `gemini-3.1-flash-lite`(저가) vs `gemini-3.5-flash`(표준) A/B 테스트: 정상/위장된 인젝션·범위확장·반복호출/오탐 후보 등 9개 케이스 전부 두 모델 결과 완전히 동일 → 정확도 차이 없음을 확인하고 **비용 저렴한 `gemini-3.1-flash-lite` 유지**로 최종 확정
- 비용 실측(Count Tokens API, 무료): 판단 1회당 입력 1,318토큰, 약 0.6~0.7원 수준. 일일 최대 50회 호출해도 약 30원 → 예산 제약 아님을 확인

**중요한 구조적 한계 (팀 전체가 인지해야 함)**
- 레이어2는 AI가 스스로 작성한 근거 텍스트를 신뢰하는 구조라, 완전히 매끄럽게 조작된 거짓 근거까지는 원리적으로 못 잡음
- 따라서 **레이어2를 최종 방어선으로 취급하면 안 됨** — 실제 금전적 피해 상한은 항상 레이어1의 하드 규칙(`per_tx_limit`, `daily_limit`)이 결정함. 피칭/데모 설명 시에도 "레이어2는 best-effort 정밀 판단, 레이어1이 hard cap"으로 정직하게 설명할 것
- 사용자의 **최초 작업 요청 자체**는 검사 대상이 아님(스코프의 정의 기준점 자체라 자기 자신과 비교하는 셈이라 논리적으로 불필요) — 검사 대상은 그 이후 체인에서 AI가 만들어내는 개별 호출들

## 현재까지 진행 상황
- ✅ 정책 판단 엔진 레이어1 완성 (`policy_engine.py`)
  - `PolicyConfig`: 정책 숫자 설정 (일일한도, 건당한도, 허용카테고리, 최대호출횟수, 분당호출한도, AI연속실패한도, 공식 결제대상 주소)
  - `PaymentRequest`: 판단에 필요한 입력 전체(금액/카테고리 + 지갑·API·권한·인프라 상태값)
  - `SpendTracker`: 일일 누적 지출/호출 추적 (자정 리셋)
  - `RequestGuard`: 중복 요청(request_id) 및 분당 레이트리밋 추적
  - `PolicyEngine.evaluate()`: 보안 파트너가 정의한 14개 거부조건 중 10개(2,3,4,5,6,7,8,9,13,14) 판단
  - 테스트 10개 시나리오 전부 통과 확인 (정상승인/건당초과/카테고리불허/지갑미연결/잔액부족/주소불일치/중복결제/AI반복실패/레이트리밋/일일누적초과)
- ✅ 정책 판단 엔진 레이어2 완성 (`semantic_layer.py`)
  - `SemanticGuard`: Gemini(`gemini-3.1-flash-lite`, 모델 선정 배경은 아래 "레이어2 재설계" 참고) 호출해 나머지 4개 조건(1,10,11,12) 판단
  - Gemini 호출 실패 시 조건14(인프라 불안정)로 간주해 자동 거부(fail-safe) 처리
  - AI-to-AI 컨텍스트에 맞게 few-shot·판단 기준 재설계, `task_plan` 필드로 인젝션 타이밍 방어 추가 (상세는 아래 "레이어2 재설계" 섹션)
- ✅ 정보보안 친구 실제 정책 스펙 반영 완료: 일일한도 10 USDC, 건당한도 2 USDC, 허용카테고리 ["gemini", "bigquery"], 최대호출 50회/일, 분당 5회, AI연속실패 3회 (`main.py`, `policy_engine.py` 테스트 모두 갱신)
  - ⚠️ 공식 결제대상 주소(official_recipient_addresses)는 아직 실제 Solana 주소 문자열을 못 받아서 `main.py`에 `TODO_..._공식_솔라나_주소` 플레이스홀더로 남아있음 → 받는 즉시 교체 필요 (교체 전까지는 조건6 검사가 사실상 항상 통과됨에 주의)
- ✅ 아키텍처 3차 정정 완료 (최종 확정 버전 위 참고)
- ✅ 백엔드 연동 방식 확정: REST API(FastAPI)로 결정, `main.py`에 `POST /evaluate`, `GET /health` 구현, 레이어1→레이어2 순서로 오케스트레이션 완료
- ✅ `requirements.txt` 추가 (`fastapi`, `uvicorn`, `pydantic`, `google-genai`)

## 다음 할 일
- [x] 정보보안 친구 실제 정책 스펙 받아서 `PolicyConfig` 값(한도 숫자, 허용카테고리) 교체 완료
- [ ] 공식 결제대상 주소(Gemini/BigQuery 실제 Solana 지갑 주소 문자열) 받아서 `main.py`의 TODO 플레이스홀더 교체 — 아직 못 받음
- [x] 백엔드 친구와 연동 방식 확정: REST API로 감쌀지, 함수 직접 import해서 쓸지 → REST API(FastAPI, POST /evaluate)로 확정, main.py 구현 완료
- [x] 레이어2 LLM 기반 이상 패턴 판단 추가 → Gemini 연동 완료 (`semantic_layer.py`), `GEMINI_API_KEY` 확보 및 실제 API 테스트 완료
- [x] 백엔드 친구와 확장된 `PaymentRequest` 입력 필드명 최종 합의 자료 작성 완료 → [BACKEND_연동_가이드.md](BACKEND_연동_가이드.md) (필드별 출처·시점 정리, `task_plan` 포함). ⚠️ 문서만 작성됨, 백엔드 친구에게 실제 전달 및 합의는 아직
- [ ] `task_plan` 기반 "계획에 남은 단계 없으면 거부" 로직을 `semantic_layer.py` 프롬프트에 명시적으로 강화 (설계 확정, 구현 미완성)
- [ ] 백엔드 코드와 실제 연동 테스트 (가격확인 → AI판단 → 결제 흐름 통합)
- [ ] (선택) 정보보안 친구에게 이번에 레이어2 재설계하며 발견한 구조적 한계(자기 근거 조작 불가 탐지) 공유 — 레이어1 hard cap이 최종 방어선임을 팀 전체가 인지하도록

## VS Code / Claude Code에서 요청할 것 (예시)
"위 기획 기준으로, policy_engine.py를 FastAPI REST 엔드포인트로 감싸줘. 백엔드가 POST 요청으로 {amount, category} 보내면 위 JSON 포맷으로 응답하게."
